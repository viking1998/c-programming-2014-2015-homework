#include<stdio.h>

	int main(){
	long long unsigned int q,l1,l0,i,m,j,p;
		l1=1;
		l0=2;
		i=0;
		p=1;
		printf("%llu %llu",l0,l1);
		if (i=0);{
		do {	
		q=l1+l0;
		m=l1;
		i=i+1;
		printf(" %llu",q);
		} while (i!=1); }
				
		if (i>=1);{
		do {		
		j=q+m;
		m=q;
		q=j;
		i=i+1;
		printf(" %llu",j);
		} while (i!=99);}
		

	return 0;
}
