#include <stdio.h>
int main ()
{
	long long int a=2;
	long long int b=1;
	long long int c;
	int i;
        int z;
	printf("%lld(1)",a);
	z = 2;
	printf(", %lld(2)",b);
	
	for (i = 2; i < 100; i ++){
		z = z + 1;
 		c = a + b;
		printf(", %lld(%d)",c,z);
		a = b;
		b = c;}
	return 0;
}
